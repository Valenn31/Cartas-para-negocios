[build]
command = "cd backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && python manage.py migrate && python manage.py collectstatic --noinput && waitress-serve --port=$PORT carta_restaurantes.wsgi:application"

[environments.production]
# Las variables de entorno se configuran en el dashboard de Railway